name: Dependency Audit

on:
  # Only when the dependencies themselves change. A schedule would re-audit an
  # unchanged tree and fail on newly published CVEs, which reads as a broken
  # build on a repo nobody has touched.
  push:
    paths:
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/audit.yml"
  pull_request:
    paths:
      - "pyproject.toml"
      - "uv.lock"
  # Still runnable on demand from the Actions tab.
  workflow_dispatch:

jobs:
  audit:
    name: "pip-audit (CVE scan)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Set up uv"
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true

      - name: "Export requirements for audit"
        # pip-audit works best against a flat requirements list
        # --no-emit-project drops the `-e .` self-reference: every other line
        # carries --hash, which puts pip in hash-checking mode, and an editable
        # install has no single file to hash.
        run: uv export --no-dev --no-emit-project --format requirements-txt > /tmp/requirements.txt

      - name: "Run pip-audit"
        run: uvx pip-audit -r /tmp/requirements.txt
